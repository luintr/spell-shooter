// Convert radians to degrees
export function radToDeg(angle: any) {
  return angle * (180 / Math.PI);
}

// Convert degrees to radians
export function degToRad(angle: any) {
  return angle * (Math.PI / 180);
}